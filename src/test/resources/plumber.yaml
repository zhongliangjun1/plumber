view:
    resourcesLocation: classpath
    resourcesPath: /view
    suffix: .ftl
    encoding: UTF-8
    viewSourceLoaderFactory: com.dianping.plumber.view.ViewSourceLoaderFactory
    viewRendererFactory: com.dianping.plumber.view.supportemarkerRendererFactory

business:
    controllersPackageScan:
    pipesPackageScan:

concurrent:
    timeout: 1000
    threadPool:
        corePoolSize: 50
        maximumPoolSize: 50
        keepAliveTime: 0
        blockingQueueCapacity: 1000